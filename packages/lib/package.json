{
	"name": "automod-lib",
	"version": "1.0.0",
	"type": "module",
	"description": "Shared code for AutoMod",
	"scripts": {
		"clean": "rm -rf dist",
		"build": "bun run clean && bunx --bun tsc -p tsconfig.json",
		"dev": "bunx --bun tsc -p tsconfig.json -w --preserveWatchOutput"
	},
	"devDependencies": {
		"@types/bun": "latest"
	}
}
